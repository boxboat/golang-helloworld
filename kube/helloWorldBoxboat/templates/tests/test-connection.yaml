apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helloWorldBoxboat.fullname" . }}-test-connection"
  labels:
    {{- include "helloWorldBoxboat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helloWorldBoxboat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
